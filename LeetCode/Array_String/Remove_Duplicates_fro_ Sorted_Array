class Solution {
    func removeDuplicates(_ nums: inout [Int]) -> Int {
        var tmp = Set<Int>()
        
        for value in nums {
            tmp.insert(value)
        }
        
        let tmp2 = Array(tmp).sorted()
        nums = tmp2
        return nums.count
    }
}
